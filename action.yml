name: clank8y
description: Reviews your pull requests for Cumulocity IoT projects 
author: schplitt

inputs:
  prompt:
    description: Optional event instruction block appended to the base review prompt
    required: false

runs:
  using: node24
  main: dist/index.mjs
